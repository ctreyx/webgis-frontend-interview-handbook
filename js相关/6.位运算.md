const PERMISSION = {
  VIEW:   0b00001,   // 1
  ADD:    0b00010,   // 2
  EDIT:   0b00100,   // 4
  DELETE: 0b01000,   // 8
}

// 1. 判断是否有某个权限
const hasView = (userPermission & PERMISSION.VIEW) !== 0

// 2. 新增权限
userPermission = userPermission | PERMISSION.EDIT

// 3. 删除权限
userPermission = userPermission & ~PERMISSION.EDIT

// 4. 判断是否同时拥有多个权限
const required = PERMISSION.VIEW | PERMISSION.ADD
const hasAll = (userPermission & required) === required

// 5. 判断是否拥有任意一个权限（补充）
const hasAny = (userPermission & required) !== 0