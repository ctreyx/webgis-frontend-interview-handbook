我们低代码平台需要支持多套主题切换，包括暗色模式和多品牌色定制。

这个换肤系统是我从 0 到 1 主导设计的，核心思路是把所有视觉属性抽象为 CSS 变量，运行时通过 JS 动态注入到 :root 上实现毫秒级切换。

做的过程中有几个关键问题需要解决。

第一个问题是颜色复用。 气象大屏里同一个颜色需要很多透明度变体，比如背景色 100% 不透明、边框 70%、阴影 30%。如果每个透明度都手动定义一个变量，维护成本很高。我的方案是通过 computed 自动生成——一个基础 RGB 值自动派生出 0 到 1 共 11 级透明度梯度变量，业务组件直接用 var(--backgroundColor3) 就是 30% 透明度，不需要手动计算 rgba。这样新增一个颜色，11 个透明度变体自动就有了。

第二个问题是主题扩展性。 一开始只有两套主题，但后来业务要加第三套。如果写 if-else 切换，每加一套主题就要改切换逻辑。我用取模循环来做——主题存在一个 map 里，切换时取当前索引加 1 对总数取模，新增主题只需要往 map 里加一条配置，切换逻辑零改动。

第三个问题是持久化和防闪烁。 用户选了主题，刷新页面要保持。持久化用 LocalStorage 很简单，但如果在 Vue 加载完才读取主题注入变量，用户会先看到默认主题闪一下再切换。我的方案是在 index.html 的 <head> 里放一段内联阻塞脚本，在 CSS 解析之前就从 LocalStorage 读取主题并注入变量到 :root，这样浏览器构建 CSSOM 的时候主题变量已经存在了，不会闪烁。